# >>> nexa arch-unit >>>
# Installed by the /setup-arch-unit skill. Runs the project's architecture
# rules before each commit. Idempotent: re-running setup-arch-unit replaces
# only the lines between these markers.
npm run test:arch
# <<< nexa arch-unit <<<
